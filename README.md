# 쁠리 후원 랭킹 (핑크 테마, 카테고리 탭)

- 상단 탭으로 **총후원개수 / 참여율 / 금액** 기준 랭킹 전환
- 헤더 클릭 정렬, 검색, 시트 필터, 컬럼 토글
- 밝은 분홍 그라데이션 테마, 테마 선택 기능 제거

## 배포
1. 이 폴더의 4개 파일(`index.html`, `style.css`, `script.js`, `README.md`)을 저장소에 업로드(덮어쓰기).
2. Settings → Pages에서 main/(root) 설정.
3. `https://<유저명>.github.io/<저장소명>/` 접속.
